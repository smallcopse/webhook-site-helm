## Global settings
namespace: webhook-site

## webhook.site (Laravel app) settings
webhook:
  image:
    repository: webhooksite/webhook.site
    tag: latest
    pullPolicy: IfNotPresent
  replicas: 1
  service:
    port: 8084
    targetPort: 80
  ## Name of an existing Secret with key APP_KEY.
  ## If set, the chart will NOT create a Secret and will use this instead.
  existingSecret: ""
  ## Override APP_URL explicitly.
  ## When left empty the chart auto-derives it in this order:
  ##   1. route.host  (if set)
  ##   2. OpenShift IngressController lookup → webhook-<namespace>.<apps-domain>
  ## Set this only when auto-detection is not desired.
  appUrl: ""
  env:
    APP_ENV: production
    APP_DEBUG: "false"
    ## Used when existingSecret is empty. Generate with: php artisan key:generate --show
    APP_KEY: ""
    DB_CONNECTION: sqlite
    BROADCAST_DRIVER: redis
    CACHE_DRIVER: redis
    QUEUE_DRIVER: redis
    REDIS_HOST: redis
    REDIS_PORT: "6379"
  resources: {}
  #  requests:
  #    cpu: 100m
  #    memory: 128Mi
  #  limits:
  #    cpu: 500m
  #    memory: 512Mi

## Laravel Echo Server settings
echoServer:
  image:
    repository: webhooksite/laravel-echo-server
    tag: latest
    pullPolicy: IfNotPresent
  replicas: 1
  service:
    port: 6001
  env:
    ECHO_ALLOW_CORS: "true"
    ECHO_ALLOW_ORIGIN: "*"
    ECHO_ALLOW_METHODS: "*"
    ECHO_REDIS_HOSTNAME: redis
    ECHO_REDIS_PORT: "6379"
    ECHO_SERVER_HOST: "0.0.0.0"
    ECHO_SERVER_PORT: "6001"
    ECHO_PROTOCOL: http
    ## Points to the webhook service; override if using a custom service name
    ECHO_AUTH_HOST: http://webhook:8084
  resources: {}

## Redis settings
redis:
  image:
    repository: redis
    tag: alpine
    pullPolicy: IfNotPresent
  replicas: 1
  service:
    port: 6379
  resources: {}

## OpenShift Route settings (replaces Ingress)
route:
  ## Hostname for the Route (e.g. webhook.apps.mycluster.example.com)
  ## Leave empty to let OpenShift auto-assign
  host: ""
  ## TLS termination: edge | reencrypt | passthrough | "" (no TLS)
  tls:
    enabled: false
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
